# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Feed' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Feed

  pod 'Parse'
  pod 'Bolts'
  target 'FeedTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FeedUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
